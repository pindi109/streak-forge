# StreakForge

> Build habits. Break limits. Every single day.

StreakForge speaks to driven self-improvers and productivity enthusiasts who want tangible proof of their daily consistency. The brand is focused, empowering, and quietly intense — like a personal coach who celebrates your wins without the fluff. It blends the precision of data with the warmth of personal growth.

## Description
Build a habit tracker app with daily streaks, reminders and progress charts

## Tech Stack
| Layer | Technology |
|-------|-----------|
| Frontend | Flutter (Dart) |
| State Management | Provider |
| Auth | Firebase Auth (email/password + Google Sign-In) |
| Database | Cloud Firestore |
| Storage | Firebase Storage |
| Push Notifications | Firebase Cloud Messaging |
| Backend | Firebase Cloud Functions |

## Folder Structure
```
lib/
  core/           # Theme, constants, utilities
  features/
    auth/         # Login, register, auth provider/service
    home/         # Main app screens and widgets
  shared/         # Reusable widgets
  main.dart
website/          # Landing page HTML (index, privacy, terms, contact)
branding/         # Brand assets and logo
```

## Getting Started

### Prerequisites
- Flutter SDK >= 3.0.0
- Firebase CLI
- A Firebase project (add `google-services.json` to `android/app/`)

### Installation
```bash
git clone https://github.com/pindi109/streak-forge
cd streakforge
flutter pub get
flutter run
```

### Environment Setup
1. Create a Firebase project at [console.firebase.google.com](https://console.firebase.google.com)
2. Enable Authentication (Email/Password + Google)
3. Enable Firestore and Storage
4. Download `google-services.json` → place in `android/app/`
5. Download `GoogleService-Info.plist` → place in `ios/Runner/`

## Website
Static landing pages are in `/website/`. Deploy to Firebase Hosting, Netlify, or Vercel.

---
*Generated by [AI App Builder](https://github.com/futurebuildcov/ai-app-builder)*
